<?php 
    //basic operators
    $x = 70;
    $y = 65;
    $add = $x + $y;
    $sub = $x - $y;
    $dev = $x / $y;
    $multi = $x * $y;
    $mod = $x % $y;
    echo "$add || $sub || $dev || $multi || $mod"
?>